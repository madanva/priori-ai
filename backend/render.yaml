services:
  - type: web
    name: priori-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PORT
        value: 10000
      - key: OLLAMA_MODEL
        value: llama3.2
    healthCheckPath: /api/health
